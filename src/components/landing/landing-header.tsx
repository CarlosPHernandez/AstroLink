'use client';

import { PublicSiteHeader } from '@/components/landing/public-site-header';

/** Mission landing header — shared public chrome with landing tokens. */
export function LandingHeader() {
  return <PublicSiteHeader variant="landing" />;
}
