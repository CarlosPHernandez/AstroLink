# Weekly ops Slack post (#astrolink-ops)

Carlos posts every **Monday** after refreshing [`weekly-team-brief.canvas.tsx`](file:///Users/carloshernandez/.cursor/projects/Users-carloshernandez-Development-Code-Projects-AstroLink/canvases/weekly-team-brief.canvas.tsx) in Cursor.

## Before posting

1. In Cursor: `/canvas Refresh weekly-team-brief from Supabase early_access_signups`
2. Open the canvas beside chat; confirm stats match [admin waitlist](/dashboard/admin) (Phase 2).
3. Screenshot: top **Stat** row only (or full canvas if a chart has data).

## Message template

Copy into `#astrolink-ops` (or DM Oliver + Zen):

```text
AstroLink weekly — <YYYY-MM-DD>

[paste screenshot]

What changed
• <one sentence on waitlist total / 7d trend>

Oliver (sales)
• <one action: e.g. follow up referrer partner-nasa, prep call with X>

Zen (marketing)
• <one action: e.g. ship next post with ref=linkedin-jun-2026>

Admin (live): https://<host>/dashboard/admin
Canvas (refresh): weekly-team-brief.canvas.tsx
```

## When to use other canvases

| Ask from | Canvas | When |
|----------|--------|------|
| Oliver | `sales-pipeline` | Before sales sync or expert outreach week |
| Zen | `marketing-funnel` | Within 48h of a campaign ending |
| Carlos | `platform-health` | Before customer demo or production promote |

## Rules

- Three bullets max per person; details live in the screenshot or admin.
- If a chart has no data yet, say so in text — do not post an empty chart image.
- Do not paste raw emails in Slack; use counts and referrer names only.
